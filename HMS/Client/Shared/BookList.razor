@if (book == null)
{
    <text>Loading Items.....</text>
}
else if (book.Count == 0)
{
    <text>There are no books to display</text>
}
else
{

    <input type="checkbox" @onchange ="@(() => displayButtons =!displayButtons )"/>
    @foreach (var book_ in book)
    {
        <IndividualBookList book=" book_ " DisplayButtons="displayButtons"></IndividualBookList>
        <div>
           
        </div>
    }
}

@code {
    [Parameter] public List<Books> book { get; set; }
    bool displayButtons = false;



}
